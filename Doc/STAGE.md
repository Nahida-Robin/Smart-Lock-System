1. 系统构思：密码验证(模块内部逻辑完善，外部调用函数根据返回值作判断)、开锁(四相八拍步进电机、舵机)、传感器数据(DHT11、光敏、红外)、声光报
警(蜂鸣器、LED)、OLED交互(实时显示+按键调试)、串口(接收中断+串口重定向)蓝牙(接收中断+自封装阻塞发送函数)调试

2. 模块化编写大体框架，最后统一TaskDriver调度，写完传感器逻辑并验证，OLED驱动移植成功，串口蓝牙调试成功

3. 编写密码相关逻辑(Password.c/.h)，考虑到0-9再加上确认删除等按键较多，想到矩阵键盘可以省一半IO口，到EDA软件画原理图，PCB打板焊接，编写行拉高，列读取的驱动，验证

4. 编写边验证完善整体框架，整个系统测试

5. 修正密码残留显示问题

6. 修正读取历史密码时数组索引混用(Uw_Index、Tm_Index)

7. 修正直接返回未清空Userword用户数组和Vertify认证状态

8. 新增OLED只显示历史密码最新三条

9. 传感器读取状态机化，TIM定时器定时触发传感器读取

10. 添加Page变量，OLED显示可翻页，不再只有三条

11. 修复阈值调节减少时uint类型变量下溢风险

12. 串口匹配命令后直接return，否则会一直走完所有匹配链，提高效率

13. 新增Flash(W25Q16)读写历史记录，三个地址存记录，一个地址存各记录长度，一个地址存是否是初次读写

14. 只在文件内部调用的函数static，限制作用域

15. 优化串口，串口接收改用DMA搬运+空闲中断，串口命令处理改用表驱动(无参表和有参表)

16. 新增RTC时钟，掉电数据不丢失

17. 修复RTC获取时间传参错误问题

18. 状态机新增状态MENU，将时间和传感器数据分开，可以通过矩阵键盘修改时间

19. 修改日期时新增根据月份匹配日期上下限以及是否是闰年判断2月有几天

20. 修复Flash擦除数据后所有数据读取失败